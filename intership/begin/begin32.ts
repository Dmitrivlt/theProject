// Begin32. A centigrade temperatureT is given. Convert it into a Fahrenheit temperature. The centigrade temperature TC and the Fahrenheit temperature TF are connected as:
// TC = (TF − 32)·5/9.

const c = 21;
let f = (c * 5 / 9) + 32
console.log(f)