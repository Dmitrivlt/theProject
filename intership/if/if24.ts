// If24. Given a real independent variable x, find the value of a real function f defined as:
// f(x) = 2·sin(x), if x > 0, 6 − x, if x ≤ 0.

// If24. Для данного вещественного x найти значение следующей функции f,
// принимающей вещественные значения:
// f (x) = 2·sin(x), если x > 0, 6−x, если x ≤ 0.

let x = 42
let f = 0
if (x > 0) {
  console.log(f = 2 * Math.sin(x))
} else {
  console.log(f = 6 - x)
}
